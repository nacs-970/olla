# Phase 02.1: API connect with olla, not just ollama now - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-04
**Phase:** 02.1-api-connect-with-olla-not-just-ollama-now
**Areas discussed:** Provider Architecture, Auth & Configuration, API Request Contract, Ollama Coexistence

---

## Provider Architecture

| Option | Description | Selected |
|--------|-------------|----------|
| OpenAI-compatible client interface (OpenRouter) | Unifies OpenAI, OpenRouter, Groq, vLLM via `httpx` | ✓ |
| Official provider SDKs | Dedicated clients per provider (`openai`, `anthropic`, `google-genai`) | |
| All-in-one gateway (`litellm`) | Large multi-provider library | |

| Option | Description | Selected |
|--------|-------------|----------|
| `httpx` | Lightweight, connection pooling, clean timeouts | ✓ |
| `urllib.request` | Standard library, 0 extra dependencies | |
| `openai` Python SDK | Official package pointed to OpenRouter base_url | |

| Option | Description | Selected |
|--------|-------------|----------|
| Generic OpenAI-compatible protocol with OpenRouter preset | Defaults to OpenRouter, allows custom base URLs | ✓ |
| Strictly OpenRouter-only | Hardcoded endpoint | |

| Option | Description | Selected |
|--------|-------------|----------|
| Streaming | Stream tokens in real time, buffer stop sequence | ✓ |
| Non-streaming | Simple block responses matching current Ollama calls | |
| You decide | Agent picks | |

---

## Auth & Configuration

| Option | Description | Selected |
|--------|-------------|----------|
| Environment variables + CLI override | `OPENROUTER_API_KEY`/`OPENAI_API_KEY` + `--api-key` | ✓ |
| Environment variables only | No CLI key flag | |
| CLI flag only | `--api-key` required | |

| Option | Description | Selected |
|--------|-------------|----------|
| Prefix syntax in `--model` | `openrouter/...` or `openai/...` | ✓ |
| Explicit `--provider` flag | Separate flag | |
| Auto-detect | Implicit based on environment/model name | |

| Option | Description | Selected |
|--------|-------------|----------|
| `--base-url` flag + `OPENAI_BASE_URL` env var | Custom endpoints supported | ✓ |
| Pre-configured presets only | Fixed endpoints only | |

| Option | Description | Selected |
|--------|-------------|----------|
| Immediate failure with clear error | Exit non-zero immediately with diagnostic | ✓ |
| Interactive prompt | Prompt in terminal | |

---

## API Request Contract

| Option | Description | Selected |
|--------|-------------|----------|
| Server `stop` parameter + client stream window buffer | Send `stop: ["</args>"]`, suppress partial stop tags | ✓ |
| Server-side only | Trust provider to terminate | |
| Client-side cutoff only | Break reader manually | |

| Option | Description | Selected |
|--------|-------------|----------|
| Pull from each model detail | Dynamically query model details for limits | ✓ |
| Default `max_tokens=4096` | Static default | |
| Do not specify `max_tokens` | Rely on server defaults | |

| Option | Description | Selected |
|--------|-------------|----------|
| 60s timeout with automatic retry on 429/5xx (3 retries) | Robust retry policy | ✓ |
| Fail-fast (30s timeout, no retries) | Immediate exit | |
| User configurable | Flag only | |

| Option | Description | Selected |
|--------|-------------|----------|
| Dimmed/styled thinking stream | Dimmed `<think>`, parse `<tool>` outside thinking | ✓ |
| Hide `<think>` blocks | Filter from stdout | |
| Raw stream | Plain text | |

---

## Ollama Coexistence

| Option | Description | Selected |
|--------|-------------|----------|
| Default to Ollama if no prefix given | Full backward compatibility | ✓ |
| Always require explicit prefix | Breaking change | |
| Auto-detect | Probe local then remote | |

| Option | Description | Selected |
|--------|-------------|----------|
| Modular provider module (`olla/providers/`) | Clean interface separation | ✓ |
| Inline dispatch in `loop.py` | Single file | |

| Option | Description | Selected |
|--------|-------------|----------|
| Support both in `--smoke-test` | Check format compliance across all models | ✓ |
| Ollama only | Local smoke test only | |

| Option | Description | Selected |
|--------|-------------|----------|
| Clean provider diagnostic | `OpenRouter error (status): message` without traceback | ✓ |
| Full traceback | Debugging output | |

---

## the agent's Discretion
- In-memory vs disk caching for OpenRouter model metadata.
- Stream buffer sliding window size.

## Deferred Ideas
- Cost tracking and token budget guards.
- Local persistent disk cache for model metadata.
