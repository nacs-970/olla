---
phase: 03-file-tools
plan: 04
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - src/olla/loop.py
  - src/olla/prompts.py
  - tests/test_loop.py
autonomous: true
requirements: [FILE-01, FILE-02]
gap_closure: true
gap_ids: [G-03-1, G-03-3]

must_haves:
  truths:
    - "Malformed shell quoting produces a concise prescriptive error in dry-run and a recoverable Observation in normal mode"
    - "The prompt prioritizes read_file/write_file over shell, forbids shell-based file I/O, and uses relative file paths"
    - "The five-tool memory contract and all existing shell/file behavior remain intact"
  artifacts:
    - path: "src/olla/loop.py"
      provides: "Small-model-friendly malformed-shell recovery wording"
      contains: "could not parse shell command"
    - path: "src/olla/prompts.py"
      provides: "File-tool-first prompt ordering, relative-path examples, and shell file-I/O prohibition"
      contains: "Always use the `read_file` and `write_file` tools instead"
    - path: "tests/test_loop.py"
      provides: "Normal and dry-run malformed-shell recovery assertions"
      contains: "could not parse shell command"
  key_links:
    - from: "src/olla/loop.py"
      to: "tests/test_loop.py"
      via: "Malformed shlex input is returned as terminal output or an Observation and asserted without executing shell"
      pattern: "mismatched quotes|could not parse shell command"
    - from: "src/olla/prompts.py"
      to: "tests/test_prompts.py"
      via: "The existing semantic prompt suite preserves the five-tool and prior-guidance contracts"
      pattern: "read_file|write_file|shell"
---

<objective>
Close the two major/blocker gaps identified during Phase 03 UAT:

1. Make `shlex.split()` failures understandable and recoverable for small models instead of returning the cryptic `could not parse command` wording.
2. Steer small models toward the dedicated file tools by placing them before shell, prohibiting shell for file I/O, and using relative paths in examples.

Purpose: improve tool selection and recovery on the project's target 0.6B-4B local models while preserving the existing parser, safety gate, and five-tool protocol.

Output: focused changes in `src/olla/loop.py`, `src/olla/prompts.py`, and `tests/test_loop.py`.
</objective>

<execution_context>
@/home/nacs/.codex/gsd-core/workflows/execute-plan.md
@/home/nacs/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-file-tools/03-UAT.md
@src/olla/loop.py
@src/olla/prompts.py
@tests/test_loop.py
@tests/test_prompts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make malformed shell quoting recoverable for small models</name>
  <files>src/olla/loop.py, tests/test_loop.py</files>
  <behavior>
    - Normal mode prints a prescriptive `could not parse shell command` message, appends the same message as an Observation, and lets the model retry without running shell.
    - Dry-run prints the same prescriptive error and stops without running shell or asking for confirmation.
    - Existing valid shell dispatch behavior remains unchanged.
  </behavior>
  <action>
Replace the two `shlex.split()` ValueError messages with wording that identifies mismatched quotes and asks the model to fix the quotes and retry. Update only the corresponding normal and dry-run assertions in `tests/test_loop.py`; preserve all surrounding dispatch logic.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_loop.py::test_run_loop_malformed_args_recovers tests/test_loop.py::test_dry_run_malformed_args_prints_error_and_stops -q</automated>
  </verify>
  <done>Both malformed-quote paths are prescriptive, tested, and side-effect free.</done>
</task>

<task type="auto">
  <name>Task 2: Prioritize dedicated file tools in the system prompt</name>
  <files>src/olla/prompts.py</files>
  <behavior>
    - The five-tool list and descriptions put read_file/write_file before shell.
    - File examples use relative paths and appear before the shell example.
    - A direct rule prohibits cat/echo/sed/awk file I/O through shell and directs the model to read_file/write_file.
    - Memory syntax, the memory round trip, final tags, and the literal `&lt;/args&gt;` warning remain present.
  </behavior>
  <action>
Reorder only the existing prompt sections and examples, replace absolute example paths with relative paths, and add the explicit shell-for-file-I/O prohibition. Do not add tools, dependencies, JSON schemas, or automatic memory behavior.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_prompts.py tests/test_loop.py -q &amp;&amp; .venv/bin/ruff check src tests</automated>
  </verify>
  <done>The concise five-tool prompt prioritizes the dedicated file tools while retaining every established protocol contract.</done>
</task>

</tasks>

<verification>
Run the focused malformed-shell tests, the prompt contract tests, the maintained full suite, Ruff, and `git diff --check` before closing the plan.
</verification>

<success_criteria>
- G-03-1 and G-03-3 are represented by a valid structured plan and matching summary.
- Malformed shell calls recover with prescriptive wording in normal and dry-run modes.
- The prompt prioritizes dedicated file tools without regressing shell or memory contracts.
- All maintained tests and lint checks pass.
</success_criteria>
