---
context: phase
phase: 01-core-loop-shell-tool-cli
task: 2
total_tasks: 4
status: blocked
last_updated: 2026-06-11T15:30:00.000Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `--model <your-model>` placeholder in checkpoint commands | Literal `<your-model>` in zsh cmd → parse error (zsh treats `<`/`>` as redirection) | advisory | Always inline real model name, never a placeholder token |
| Trusting prior-session model lists | `.continue-here.md`/HANDOFF model lists go stale — models get `ollama rm`'d between sessions for disk space | blocking | Before any multi-model run (SC6 etc), re-verify with `ollama list` AND check the daemon's actual `OLLAMA_MODELS` via `/proc/<pid>/environ` (client-side env var is irrelevant for `ollama list`) |

**Do not proceed until all boxes are checked.**

- [ ] CONSTRAINT: Trusting prior-session model lists — re-verify model availability live before SC6, don't assume the 5-model list from earlier sessions still holds.

<current_state>
Phase 01 functionally complete (audit done, 41/41 tests pass, 0 security/integration blockers — see 01-AUDIT.md). "Option A" in progress: SC6 smoke-test + 2 small loop.py fixes (WR-01, tool-dispatch) before Phase 2.

This session: confirmed the live `ollama serve` (manual, user nacs, :11434, OLLAMA_MODELS=/media/c/.ollama/) only has 2 models on disk now — `gemma4:e2b` and `evalengine/unbound-e2b:latest`. 3 of the original 5 SC6 target models (JOSIEFIED-Qwen3 0.6b/1.7b/4b, gemma4-uncensored-aggressive) are gone — likely rm'd for disk space (/media/c at 90%, ~20G avail).

Paused here (context was at 85%) to resolve SC6 model-list question with fresh budget.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Phase-01 checkpoint audit — Done. 01-AUDIT.md written, REQUIREMENTS.md corrected.
</completed_work>

<remaining_work>

- Task 2: SC6 smoke-test — BLOCKED on model-availability decision (see blockers). Once resolved:
  - Command per model: `PYTHONPATH=src .venv/bin/olla --smoke-test --model <name>`
  - Record think=False/True compliance %, D-08 sub-80% warning if any. Append to 01-AUDIT.md as new "SC6 Results" section.
  - If rescoping: only `gemma4:e2b` and `evalengine/unbound-e2b:latest` are currently on disk.
  - If re-pulling: 4 missing models ≈ 12GB total, /media/c has only ~20G free — tight.

- Task 3: Fix WR-01 dead-code in `src/olla/loop.py` (~lines 55-60). Current:
  ```python
  if "error" in result:
      combined = result["error"]
  elif "stdout" in result or "stderr" in result:
      combined = result.get("stdout", "") + result.get("stderr", "")
  else:
      combined = "(no output)"
  ```
  `run_shell` always returns stdout/stderr keys (possibly ""), so elif always true, "(no output)" never fires. Fix:
  ```python
  if "error" in result:
      combined = result["error"]
  else:
      combined = result.get("stdout", "") + result.get("stderr", "")
      if not combined:
          combined = "(no output)"
  ```
  Add/update test for real no-output success (e.g. `touch <tmpfile>` -> `Observation: (no output)`).

- Task 4: Add `parsed["tool"]` dispatch in `run_loop`. Currently `run_shell` called unconditionally for any `type=="tool"`. Minimal fix: if `parsed["tool"] != "shell"`, return `Observation: unknown tool '<name>'` instead of calling run_shell. Establishes dispatch point Phase 3 will extend.

Tasks 3-4 are small, isolated `loop.py` changes — route through `/gsd:quick` per CLAUDE.md (don't edit src/ directly outside GSD command). **Independent of Task 2 — can run first.**
</remaining_work>

<decisions_made>

- Paused before SC6 model decision to get fresh context budget (was at 85%).
- Confirmed daemon (user nacs) has correct OLLAMA_MODELS=/media/c/.ollama/ — not a server misconfig, models are genuinely gone from disk.
- systemd ollama.service crash-loop confirmed separate/non-blocking — manual server instance is the active one on :11434. Leave systemd unit alone (sudo-gated, separate task).
</decisions_made>

<blockers>
- SC6 model-list: need user decision — re-pull 4 missing models (~12GB on ~20G-avail disk), rescope to 2 present models (gemma4:e2b, evalengine/unbound-e2b:latest — note the latter wasn't in original roadmap target list), or skip SC6 for now and revisit.
- systemd ollama.service: crash-looping, sudo-gated fix, non-blocking (separate from working manual instance).
</blockers>

## Required Reading (in order)
1. `.planning/phases/01-core-loop-shell-tool-cli/01-AUDIT.md` — full checkpoint audit, SC6/WR-01/Finding-2 details
2. `.planning/REQUIREMENTS.md` — traceability table

## Infrastructure State
- `ollama serve` running manually as nacs on :11434, `OLLAMA_MODELS=/media/c/.ollama/` (confirmed via /proc/<pid>/environ).
- `/media/c/.ollama/manifests/` contains exactly 2 models: `registry.ollama.ai/library/gemma4:e2b`, `registry.ollama.ai/evalengine/unbound-e2b:latest`.
- `/media/c` partition: ~90% full, ~20G available.
- systemd `ollama.service`: crash-looping (no `/var/lib/ollama`, no `OLLAMA_MODELS` env) — separate unit, not the active server. Non-blocking, sudo required to fix.
- `.venv` has `olla` installed editable; run `PYTHONPATH=src .venv/bin/olla ...` or `PYTHONPATH=src .venv/bin/python -m pytest tests/ -q` (41/41 passing).

<context>
SC6's whole point is validating tag-compliance across the *named* small-model range (0.6B-4B, plus a 7B+ outlier). Silently swapping in `evalengine/unbound-e2b` (unknown size class) changes what SC6 actually proves. Re-pulling is the "stay faithful to roadmap" option but tight on disk. Rescoping is fastest but needs an explicit roadmap-deviation note. Skipping defers SC6 (already flagged "NEEDS RESEARCH-PHASE" in roadmap) further.
</context>

<next_action>
Start with: Ask user the SC6 path (re-pull / rescope / skip), per blockers above. If skip or deferred, proceed straight to Tasks 3-4 via `/gsd:quick` (independent, unblocked).
</next_action>
