---
context: phase
phase: 01-core-loop-shell-tool-cli
task: 4
total_tasks: 4
status: complete
last_updated: 2026-06-11T16:30:00.000Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `--model <your-model>` placeholder in checkpoint commands | Literal `<your-model>` in zsh cmd → parse error (zsh treats `<`/`>` as redirection) | advisory | Always inline real model name, never a placeholder token |
| Trusting prior-session model lists | `.continue-here.md`/HANDOFF model lists go stale — models get `ollama rm`'d between sessions for disk space | blocking | Before any multi-model run (SC6 etc), re-verify with `ollama list` AND check the daemon's actual `OLLAMA_MODELS` via `/proc/<pid>/environ` (client-side env var is irrelevant for `ollama list`) |

**Do not proceed until all boxes are checked.**

- [x] CONSTRAINT: Trusting prior-session model lists — re-verified live (`ollama list` + `/proc/<pid>/environ`): still only `gemma4:e2b` + `evalengine/unbound-e2b:latest` on disk. SC6 run against these 2 (rescoped, see 01-AUDIT.md SC6 Results).

<current_state>
Phase 01 functionally complete (audit done, 41/41 tests pass, 0 security/integration blockers — see 01-AUDIT.md). "Option A" in progress: SC6 smoke-test + 2 small loop.py fixes (WR-01, tool-dispatch) before Phase 2.

This session: SC6 ran (rescoped to 2 on-disk models, user-approved). Results in 01-AUDIT.md "SC6 Results" section. Headline: `gemma4:e2b` (7.2GB) crashed (`signal: killed`, status 500) — host has only 7.1GB RAM / ~3.1GB avail, model OOMs. `evalengine/unbound-e2b:latest` (3.4GB) ran fine: 100% compliant think=False, 50% think=True. SC6 marked PARTIAL/OPEN — 0.6B-4B Qwen3 risk class still unvalidated.

Only Tasks 3-4 (loop.py fixes) remain for Phase 01. Both independent, route via `/gsd:quick` per CLAUDE.md.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Phase-01 checkpoint audit — Done. 01-AUDIT.md written, REQUIREMENTS.md corrected.
- Task 2: SC6 smoke-test — Done (rescoped to 2 on-disk models). Results + open-status caveat in 01-AUDIT.md "SC6 Results" section.
</completed_work>

<remaining_work>

- Task 3: Fix WR-01 dead-code in `src/olla/loop.py` (~lines 55-60). Current:
  ```python
  if "error" in result:
      combined = result["error"]
  elif "stdout" in result or "stderr" in result:
      combined = result.get("stdout", "") + result.get("stderr", "")
  else:
      combined = "(no output)"
  ```
  `run_shell` always returns stdout/stderr keys (possibly ""), so elif always true, "(no output)" never fires. Fix:
  ```python
  if "error" in result:
      combined = result["error"]
  else:
      combined = result.get("stdout", "") + result.get("stderr", "")
      if not combined:
          combined = "(no output)"
  ```
  Add/update test for real no-output success (e.g. `touch <tmpfile>` -> `Observation: (no output)`).

- Task 4: Add `parsed["tool"]` dispatch in `run_loop`. Currently `run_shell` called unconditionally for any `type=="tool"`. Minimal fix: if `parsed["tool"] != "shell"`, return `Observation: unknown tool '<name>'` instead of calling run_shell. Establishes dispatch point Phase 3 will extend.

Tasks 3-4 are small, isolated `loop.py` changes — route through `/gsd:quick` per CLAUDE.md (don't edit src/ directly outside GSD command). **Independent of Task 2 — can run first.**
</remaining_work>

<decisions_made>

- User chose: rescope SC6 to the 2 on-disk models rather than re-pull (~12GB, tight on 21G-avail disk) or skip.
- Confirmed daemon (user nacs) has correct OLLAMA_MODELS=/media/c/.ollama/ — not a server misconfig, models are genuinely gone from disk.
- systemd ollama.service crash-loop confirmed separate/non-blocking — manual server instance is the active one on :11434. Leave systemd unit alone (sudo-gated, separate task).
- SC6 recorded as PARTIAL/OPEN (not satisfied-as-written) per advisor guidance — rescope changes what SC6 proves, doesn't make it pass.
</decisions_made>

<blockers>
- systemd ollama.service: crash-looping, sudo-gated fix, non-blocking (separate from working manual instance).
- NEW: gemma4:e2b (7.2GB) cannot run on this host (7.1GB RAM, OOM-killed). Affects which models are realistically usable for future dev/testing — not blocking Tasks 3-4.
</blockers>

## Required Reading (in order)
1. `.planning/phases/01-core-loop-shell-tool-cli/01-AUDIT.md` — full checkpoint audit, SC6/WR-01/Finding-2 details
2. `.planning/REQUIREMENTS.md` — traceability table

## Infrastructure State
- `ollama serve` running manually as nacs on :11434, `OLLAMA_MODELS=/media/c/.ollama/` (started this session, was down at session start; daemon restart loses no state — models on disk unaffected).
- `/media/c/.ollama/manifests/` contains exactly 2 models: `registry.ollama.ai/library/gemma4:e2b` (7.2GB, OOMs on this host), `registry.ollama.ai/evalengine/unbound-e2b:latest` (3.4GB, runs fine).
- `/media/c` partition: ~90% full, ~21G available.
- Host RAM: 7.1GB total, ~3.1GB avail, swap 2.9/7GB used — gemma4:e2b (7.2GB) does not fit.
- systemd `ollama.service`: crash-looping (no `/var/lib/ollama`, no `OLLAMA_MODELS` env) — separate unit, not the active server. Non-blocking, sudo required to fix.
- `.venv` has `olla` installed editable; run `PYTHONPATH=src .venv/bin/olla ...` or `PYTHONPATH=src .venv/bin/python -m pytest tests/ -q` (41/41 passing).

<context>
SC6 done (rescoped, partial/open per 01-AUDIT.md). Only Tasks 3-4 remain for Phase 01 — both small, isolated `loop.py` edits, independent of each other and of SC6.
</context>

<next_action>
Phase 01 fully complete (audit, SC6 partial/open, tasks 3-4 done — commits f967c22, 7f69865, merged bad449a, docs 97ae518). 42/42 tests pass. SC6 caveat remains in 01-AUDIT.md for future revisit (re-pull/re-run when smaller models available). Ready to advance to Phase 2 (discuss/plan).
</next_action>
